export interface LocationsState {
  file: number;
}

// NOTE Make sure that the value has a file with the same name in the assets folder
export const initialLocationsState: LocationsState = {
  file: 1,
};
